spring:
  datasource:
    url: jdbc:h2:~/data/jpashop
    username: su
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
